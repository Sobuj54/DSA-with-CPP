#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, y;
    cin >> x >> y;

    if (x == y)
        cout << x << "\n";
    else if (x > y)
        cout << y << "\n";
    else
        cout << x << "\n";
}
